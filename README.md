1. Install miniconda, git and vscode;
2. Enter your folder to save these code, git clone https://github.com/KyoungLab-UMBC/image_analysis/;
3. conda env create -f img_analysis.yml;
4. vscode will recognize the setting.json automatically, if not, change the env manually instead;
